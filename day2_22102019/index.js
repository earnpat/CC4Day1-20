let res=0, ar=(num) => document.getElementById("result").innerHTML = (num==="add") ? res+=1 : res-=1;

/*
let res=0;
function ar(num) {
    if (num === "add") {
        res = res+1;
    }
    if (num === "rem") {
        res = res-1;
    }
    document.getElementById("result").innerHTML = res;
}
*/ 
